import 'package:flutter/material.dart';

class OrderStatePage extends StatelessWidget {
  const OrderStatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
