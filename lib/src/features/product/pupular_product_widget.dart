import 'package:flutter/material.dart';

class PupularProductWidget extends StatelessWidget {
  const PupularProductWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
